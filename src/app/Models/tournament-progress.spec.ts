import { TournamentProgress } from './tournament-progress';

describe('TournamentProgress', () => {
  it('should create an instance', () => {
    expect(new TournamentProgress()).toBeTruthy();
  });
});
